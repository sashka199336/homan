input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

output {
  stdout { codec => rubydebug }   # вывод прямо в консоль Logstash
  kafka {
    bootstrap_servers => "kafka:29092"
    topic_id => "logs"
  }
}
